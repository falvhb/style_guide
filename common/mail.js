var nodemailer = require('nodemailer');

/**
 * Exports a mail transport using the local postfix.
 *
 * Usage: see https://github.com/andris9/Nodemailer
 */
module.exports = nodemailer.createTransport({});
